# Core Dependencies
fastapi>=0.104.0  # FastAPI for web server
uvicorn>=0.24.0  # ASGI server
python-multipart>=0.0.6  # For file uploads
python-dotenv>=1.0.0  # For environment variable management

# Vector Database
pinecone-client>=3.0.0  # Pinecone client for vector database

# LangChain
langchain>=0.1.0  # Core LangChain
langchain-community>=0.0.10  # Community integrations
langchain-core>=0.1.0  # Core components
langchain-text-splitters>=0.0.1  # Text splitting utilities

# Document Loaders
unstructured[local-inference]>=0.12.0  # For handling various document formats
pypdf>=3.0.0  # For PDF processing
python-docx>=1.0.0  # For Word documents
openpyxl>=3.1.0  # For Excel files

# Utilities
tqdm>=4.65.0  # Progress bars
python-magic-bin>=0.4.14; platform_system == 'Windows'  # Windows-specific binary
python-magic>=0.4.27; platform_system != 'Windows'  # File type detection

# Dependencies for unstructured
lxml>=4.9.0  # Required for XML and HTML processing
chardet>=5.1.0  # Character encoding detection

# Required for Windows compatibility
# Note: On Windows, you might need to install additional system dependencies
# For python-magic, you can use: pip install python-magic-bin
