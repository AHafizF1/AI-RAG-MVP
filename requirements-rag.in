# Core RAG dependencies
langchain>=0.1.11
langchain-google-genai>=0.0.11
langchain-text-splitters>=0.0.1
langchain-core>=0.1.0

# Vector store with hybrid search support
pinecone-client>=3.2.2,<4.0.0  # Latest stable version with hybrid search support
pinecone-text>=0.1.0,<1.0.0  # Required for hybrid search with BM25
langchain-pinecone>=0.2.0,<0.3.0  # Version with hybrid search support

# FastAPI for serving (optional, remove if not needed)
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# Document loaders (keep only what you need)
unstructured>=0.12.0,<0.13.0  # General document parsing
pypdf>=3.0.0,<4.0.0            # For PDF files
python-docx>=1.0.0,<2.0.0      # For Word documents
openpyxl>=3.1.0,<4.0.0         # For Excel files

# Utilities (optional, keep as needed)
tqdm>=4.65.0,<5.0.0            # Progress bars
python-magic-bin>=0.4.14,<0.5.0  # File type detection (Windows)
python-magic>=0.4.27,<0.5.0      # File type detection (non-Windows)
lxml>=4.9.0,<5.0.0              # XML/HTML parsing
chardet>=5.1.0,<6.0.0            # Character encoding detection
